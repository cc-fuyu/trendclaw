{
  "$schema": "https://docs.openclaw.ai/schema/config.json",
  "_comment": "TrendClaw — Full OpenClaw Integration Example",

  "agents": {
    "defaults": {
      "heartbeat": {
        "every": "30m",
        "target": "last",
        "activeHours": { "start": "08:00", "end": "22:00" }
      }
    }
  },

  "mcpServers": {
    "trendclaw": {
      "command": "node",
      "args": ["./node_modules/trendclaw/dist/mcp-server.js"],
      "env": {
        "OPENAI_API_KEY": "${OPENAI_API_KEY}",
        "SCOUT_LLM_BACKEND": "openai",
        "SCOUT_LLM_MODEL": "gpt-4.1-mini"
      }
    }
  },

  "plugins": {
    "trendclaw": {
      "llm": {
        "backend": "openai",
        "model": "gpt-4.1-mini"
      },
      "enableDeepMeta": true,
      "enableDiff": true,
      "language": "English",
      "topN": 10,
      "trendingPeriod": "daily",
      "contentFormats": ["digest", "tweet_thread"]
    }
  },

  "tools": {
    "alsoAllow": ["lobster", "trendclaw_scout"]
  },

  "_cron_examples": {
    "daily_digest": "openclaw cron add --name 'TrendClaw Daily' --cron '0 9 * * *' --tz 'America/New_York' --session isolated --model sonnet --announce --message 'Run trendclaw_scout with formats digest,tweet_thread'",
    "weekly_newsletter": "openclaw cron add --name 'TrendClaw Weekly' --cron '0 10 * * 1' --tz 'America/New_York' --session isolated --model opus --announce --message 'Run trendclaw_scout with period weekly, formats newsletter,blog_post'",
    "lobster_workflow": "openclaw cron add --name 'TrendClaw Pipeline' --cron '0 9 * * *' --session isolated --message '/lobster workflows/trendclaw.lobster'"
  }
}
